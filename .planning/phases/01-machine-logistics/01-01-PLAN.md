---
phase: 01
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - types.ts
  - constants.ts
  - components/MaintenanceRecordEditor.tsx
  - components/MaintenanceRecordCard.tsx
  - components/App.tsx
  - src/views/Sidebar.tsx
  - utils/costAggregation.ts
  - utils/internalReportPdf.ts
  - utils/pdfGenerator.ts
  - components/technician-portal/Step2_WorkLog.tsx
  - supabase/migrations/*_create_logistics_tables.sql
autonomous: false
requirements: []
---

<objective>
Create the database tables, TypeScript types, React hooks, UI components, route/sidebar entries, and PDF sections for the Machine Logistics Workflow — a logistics layer alongside maintenance records that tracks machine movement, replacement machines, rental costs, and transportation costs.
</objective>

<tasks>

<task id="1" type="db">
<read_first>
- supabase/migrations/ (existing migration files for naming convention)
- supabase/config.toml (project config)
- types.ts (MaintenanceRecord type)
</read_first>

<action>
Create a new Supabase migration file `supabase/migrations/YYYYMMDDHHMMSS_create_logistics_tables.sql` containing:

1. `logistics_operations` table with columns: id (bigint PK), customer_id (bigint NOT NULL), opened_by_record_id (bigint), closed_by_record_id (bigint nullable), operation_type (text NOT NULL, values: 'pickup_and_deliver'|'deliver_only'|'pickup_only'), status (text NOT NULL DEFAULT 'open', values: 'open'|'closed'), machine_category (text), machine_ownership (text), machine_type (text), replacement_machine_id (bigint), monthly_rental_price (numeric), daily_rental_price (numeric GENERATED ALWAYS AS monthly_rental_price/30 STORED), rental_duration_days (integer), rental_duration_hours (integer), rental_duration_minutes (integer), billable_days (integer), total_rental_cost (numeric), pickup_cost (numeric DEFAULT 0), return_cost (numeric DEFAULT 0), total_logistics_cost (numeric GENERATED ALWAYS AS COALESCE(total_rental_cost,0)+COALESCE(pickup_cost,0)+COALESCE(return_cost,0) STORED), internal_notes (text), created_at (timestamptz DEFAULT NOW()), updated_at (timestamptz DEFAULT NOW())

2. `company_machines` table with columns: id (bigint PK), name (text NOT NULL), category (text NOT NULL), machine_type (text), status (text DEFAULT 'available'), monthly_rental_price (numeric), notes (text), created_at (timestamptz DEFAULT NOW()), updated_at (timestamptz DEFAULT NOW())

3. RLS ENABLED on both tables with policies: "Authenticated users can SELECT/INSERT/UPDATE logistics_operations" and "Authenticated users can ALL company_machines"

4. Foreign key: logistics_operations.replacement_machine_id → company_machines(id) ON DELETE SET NULL
</action>

<acceptance_criteria>
- Migration file exists at supabase/migrations/*_create_logistics_tables.sql
- Contains both CREATE TABLE statements with all specified columns
- daily_rental_price is a GENERATED column (monthly/30)
- total_logistics_cost is a GENERATED column (rental+pickup+return)
- RLS enabled on both tables
- Foreign key from logistics_operations.replacement_machine_id to company_machines.id
</acceptance_criteria>
</task>

<task id="2" type="types">
<read_first>
- types.ts (all existing interfaces, especially MaintenanceRecord)
</read_first>

<action>
Add to `types.ts`:

1. `LogisticsOperation` interface:
```ts
export interface LogisticsOperation {
  id: number;
  customer_id: number;
  opened_by_record_id: number;
  closed_by_record_id?: number | null;
  operation_type: 'pickup_and_deliver' | 'deliver_only' | 'pickup_only';
  status: 'open' | 'closed';
  machine_category?: string;
  machine_ownership?: string;
  machine_type?: string;
  replacement_machine_id?: number | null;
  monthly_rental_price?: number;
  daily_rental_price?: number;
  rental_duration_days?: number;
  rental_duration_hours?: number;
  rental_duration_minutes?: number;
  billable_days?: number;
  total_rental_cost?: number;
  pickup_cost?: number;
  return_cost?: number;
  total_logistics_cost?: number;
  internal_notes?: string;
  created_at?: string;
  updated_at?: string;
  company_machines?: CompanyMachine; // joined relation
}
```

2. `CompanyMachine` interface:
```ts
export interface CompanyMachine {
  id: number;
  name: string;
  category: string;
  machine_type?: string;
  status: string;
  monthly_rental_price?: number;
  notes?: string;
  created_at?: string;
  updated_at?: string;
}
```

3. `LogisticsOperation` type must use string keys matching Supabase column names (snake_case). CamellCase mapping handled in the hook.
</action>

<acceptance_criteria>
- types.ts contains LogisticsOperation interface with all 24 fields
- types.ts contains CompanyMachine interface with 8 fields
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="3" type="hook">
<read_first>
- hooks/useCustomCatalog.ts (pattern reference: useMergedCatalog hook structure)
- supabaseClient.ts (import path)
- types.ts (LogisticsOperation, CompanyMachine types)
</read_first>

<action>
Create `hooks/useLogisticsOperations.ts`:

1. `useLogisticsOperations(customerId: number | null)` hook returning { operations, isLoading, error, createOperation, closeOperation, refresh }
2. `fetchOperations()` — supabase.from('logistics_operations').select('*, company_machines(*)').eq('customer_id', customerId).order('created_at', { ascending: false })
3. `createOperation(op)` — inserts new logistics_operation with status 'open', returns the created record
4. `closeOperation(id, closeData)` — updates status to 'closed', sets closed_by_record_id, rental duration fields, billable days, and total_rental_cost from closeData
5. `useCompanyMachines()` hook returning { machines, isLoading, error, addMachine, updateMachine, deleteMachine }
6. Date calculations: `calculateRentalDuration(openDate: string, closeDate: string)` → returns { days, hours, minutes }
7. `calculateDailyRentalPrice(monthly: number)` → returns monthly/30
8. Error handling: wrap Supabase errors in Error instances with meaningful messages

Export all functions and hooks.
</action>

<acceptance_criteria>
- hooks/useLogisticsOperations.ts exists
- useLogisticsOperations hook fetches, creates, and closes operations
- useCompanyMachines hook provides CRUD for company machines
- calculateRentalDuration returns correct days/hours/minutes from date strings
- calculateDailyRentalPrice returns monthly/30
- All Supabase errors wrapped in Error instances
</acceptance_criteria>
</task>

<task id="4" type="component">
<read_first>
- components/MaintenanceRecordEditor.tsx (stepper pattern, paidBy card selector)
- components/CollapsibleSection.tsx (collapsible section API)
- components/EmptyState.tsx (empty state pattern)
- components/ToastContext.tsx (useToast hook)
- hooks/useLogisticsOperations.ts (created in task 3)
- types.ts (LogisticsOperation, CompanyMachine)
</read_first>

<action>
Create `components/MachineLogisticsSection.tsx`:

Props: { customerId: number | null, recordId: number, maintenanceDate: string }
State: selectedAction ('pickup_and_deliver'|'deliver_only'|'pickup_only'|null), selectedOperationId (for closing), formData for new operation

Sections:
1. **Open Operations List** — fetch via useLogisticsOperations(customerId). Show each open operation as a card with type badge (amber), dates, machine details. "Close this operation" button on each card.

2. **Three Visual Cards** — matching paidBy selector style:
   - كرت 1: أيقونة شاحنة + "استلام ماكينة + تسليم بديلة" + "Pickup customer machine and deliver replacement"
   - كرت 2: أيقونة سهم يمين + "تسليم ماكينة بديلة فقط" + "Deliver replacement machine only"  
   - كرت 3: أيقونة سهم يسار + "استلام ماكينة العميل فقط" + "Pickup customer machine only"

3. **New Operation Form** (shown when a card is selected):
   - Machine category dropdown: ماكينة قهوة / مطحنة / أخرى
   - Machine ownership: ماكينة عميل / ماكينة شركة
   - Machine type: يدوي / أوتوماتيك / نصف أوتوماتيك
   - Replacement machine selector (from company_machines, only for deliver scenarios)
   - Monthly rental price (number input, EGP — only for deliver scenarios)
   - Pickup cost (number input, EGP)
   - Return cost (number input, EGP)
   - Internal notes (textarea)
   - Save button → createOperation() → showToast success → refresh

4. **Close Operation Confirmation** (shown when closing):
   - Auto-calculated preview: daily rate, duration, billable days, rental cost, total
   - Confirm button → closeOperation(id, closeData) → showToast → refresh

5. **Empty State** — when no operations exist and no action selected: "لا توجد عمليات لوجستية"

6. **Closed Operations** — collapsed list below open operations, showing closed operations with green status badge
</action>

<acceptance_criteria>
- MachineLogisticsSection.tsx exists
- Shows open operations with close button for each
- Three visual cards match paidBy selector style
- New operation form appears when card selected
- Replacement machine selector loads from company_machines
- Monthly rental price field only shown for pickup_and_deliver and deliver_only
- Auto-calculated cost preview shown when closing
- createOperation and closeOperation call Supabase and show toast
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="5" type="editor">
<read_first>
- components/MaintenanceRecordEditor.tsx (full file — stepper, STEPPER_STEPS, section rendering)
- components/MachineLogisticsSection.tsx (created in task 4)
</read_first>

<action>
Modify `components/MaintenanceRecordEditor.tsx`:

1. Update STEPPER_STEPS to 9 steps — insert `{ id: 7, name: 'لوجستيات الماكينات' }` between supervisor (6) and notes (was 7, now 8). Shift notes to 8, photos to 9.

2. Update STEP_TO_SECTION mapping: `7: 'logistics'`, `8: 'notes'`, `9: 'photos'`

3. Update SECTION_TO_STEP mapping accordingly.

4. Add new step 7 rendering block between step 6 (supervisor) and step 7 (notes, formerly):
   - Section header with TruckIcon + "لوجستيات الماكينات"
   - Render `<MachineLogisticsSection customerId={customerId} recordId={record.id} maintenanceDate={record.maintenanceDate} />`
   - Prev/Next navigation buttons

5. Import MachineLogisticsSection at top of file.

6. The section needs `customerId`. Check how customerId is passed or derive it from record context. If not available, add `customerId` prop to MaintenanceRecordEditorProps interface.
</action>

<acceptance_criteria>
- STEPPER_STEPS has 9 items with logistics at position 6 (id 7)
- Step 7 renders MachineLogisticsSection between supervisor and notes
- Prev/Next navigation works for all 9 steps
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="6" type="card">
<read_first>
- components/MaintenanceRecordCard.tsx (full file — collapsible sections, notes section)
- components/MachineLogisticsSection.tsx (created in task 4)
- components/CollapsibleSection.tsx (API)
</read_first>

<action>
Modify `components/MaintenanceRecordCard.tsx`:

1. Import MachineLogisticsSection and CollapsibleSection at top.

2. After the Type & Payment section and before the Notes section, insert:
```tsx
{/* Machine Logistics Section — always visible */}
<div className="pt-4 border-t border-hairline dark:border-hairline">
  <CollapsibleSection title="لوجستيات الماكينات">
    <MachineLogisticsSection
      customerId={/* derive from props or record context */}
      recordId={typeof record.id === 'number' ? record.id : 0}
      maintenanceDate={record.maintenanceDate}
    />
  </CollapsibleSection>
</div>
```

3. Extract MachineLogisticsSection as its own file (task 4) to prevent MaintenanceRecordCard from growing past 600+ lines.
</action>

<acceptance_criteria>
- MaintenanceRecordCard shows logistics section before Notes
- Logistics section is always visible (collapsible)
- Empty state shown when no operations exist
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="7" type="route">
<read_first>
- components/AppRouter.tsx (route structure)
- App.tsx (view switch statement, lazy imports)
- src/views/Sidebar.tsx (NAV_ITEMS usage, icon imports)
- constants.ts (NAV_ITEMS array)
</read_first>

<action>
1. Create `src/views/MachineSettingsView.tsx`:
   - Lazy-loaded view
   - Uses useCompanyMachines hook
   - Card-based layout with list of company machines
   - Add machine form: name, category (ماكينة قهوة/مطحنة/أخرى), type (يدوي/أوتوماتيك/نصف), status (متاحة/قيد الاستخدام/تالفة/مفقودة), monthly rental price, notes
   - Edit/delete buttons per machine
   - Empty state when no machines

2. Add to `constants.ts` NAV_ITEMS array:
```ts
{
  key: 'machines',
  label: 'إدارة الماكينات',
  iconName: 'Cog6ToothIcon',
  inSidebar: true,
}
```

3. In `App.tsx`:
   - Add lazy import: `const MachineSettingsView = lazy(() => import('./src/views/MachineSettingsView'))`
   - Add case to renderCurrentView switch: `case 'machines': return <MachineSettingsView />`

4. In `src/views/Sidebar.tsx`:
   - Import Cog6ToothIcon from @heroicons/react/24/outline
   - Add 'Cog6ToothIcon' to ICONS mapping
</action>

<acceptance_criteria>
- /settings/machines route works (via App.tsx view dispatch)
- Sidebar shows "إدارة الماكينات" nav item with gear icon
- MachineSettingsView shows list of company machines with add/edit/delete
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="8" type="view">
<read_first>
- src/views/SubmissionDetailsView.tsx (view pattern)
- hooks/useLogisticsOperations.ts (created in task 3)
- types.ts (LogisticsOperation)
</read_first>

<action>
Create `src/views/LogisticsTimelineView.tsx`:

1. Lazy-loaded view showing all logistics operations for the currently selected company
2. Timeline layout: chronological list with open (amber badge) and closed (green badge) operations
3. Each timeline entry shows: operation type, dates (opened/closed), machine details, costs (internal), linked maintenance records
4. Filter tabs: الكل / مفتوحة / مغلقة
5. Empty state when no operations
6. Uses useLogisticsOperations hook
7. Accessible from the company details page or sidebar

Add route/view key for this view in constants.ts and App.tsx.
</action>

<acceptance_criteria>
- LogisticsTimelineView.tsx exists and renders
- Shows open/closed operations in chronological order
- Filter tabs work (all/open/closed)
- Costs visible (internal view)
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="9" type="pdf">
<read_first>
- utils/internalReportPdf.ts (full file — section structure, engine pattern)
- utils/pdfGenerator.ts (customer-facing PDF, includeCosts option)
- utils/pdfTheme.ts (BRAND colors, drawTableHeader, drawTableRow, drawSectionHeader)
- utils/costAggregation.ts (AggregatedCosts type)
</read_first>

<action>
1. **Internal PDF** (`utils/internalReportPdf.ts`):
   - Add new `buildLogisticsFinancialCategories(operations)` helper that creates FinancialCategory entries for logistics costs
   - Add new section "عمليات اللوجستيات" using engine.addSection pattern
   - Logistics table columns (RTL): العملية | النوع | تاريخ الفتح | تاريخ الإغلاق | المدة | الإيجار | النقل | الإجمالي
   - Add logistics costs to AggregatedCosts: totalLogisticsRentalCost, totalLogisticsPickupCost, totalLogisticsReturnCost, totalLogisticsCost
   - Add logistics KPI card to the KPI cards row (total logistics cost)

2. **Customer PDF** (`utils/pdfGenerator.ts`):
   - In `generateCompanyPDF` and `generateBranchPDF`, when `options.includeCosts === false`:
   - Add logistics service-level section: operation type, pickup/delivery dates, machine details
   - NEVER show: rental prices, daily rates, rental costs, transportation costs, total costs, internal notes

3. **Cost Aggregation** (`utils/costAggregation.ts`):
   - Extend `AggregatedCosts` interface with: totalLogisticsRentalCost, totalLogisticsPickupCost, totalLogisticsReturnCost, totalLogisticsCost
   - Update `aggregateCosts` to include logistics costs from operations
</action>

<acceptance_criteria>
- Internal PDF shows logistics section with all costs
- Customer PDF shows logistics service info but NO costs/prices
- AggregatedCosts includes logistics cost fields
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

<task id="10" type="tech_portal">
<read_first>
- components/technician-portal/Step2_WorkLog.tsx (technician form structure)
- components/MachineLogisticsSection.tsx (created in task 4)
</read_first>

<action>
Modify `components/technician-portal/Step2_WorkLog.tsx`:

1. Import MachineLogisticsSection
2. Add logistics section after the existing maintenance fields
3. Pass customerId, recordId, maintenanceDate from technician portal context
4. Same behavior as MaintenanceRecordCard integration

Check `components/technician-portal/TechnicianPortal.tsx` for how customerId is tracked.
</action>

<acceptance_criteria>
- Technician Step2_WorkLog shows logistics section
- Section functions identically to admin editor integration
- npx tsc --noEmit passes
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run test -- --run` — all 303+ existing tests still pass
3. Migration file is valid SQL and follows existing naming convention
4. New components render without crashing (manual smoke test)
5. Logistics operations can be created/closed from the maintenance record editor
6. Logistics section appears in both admin editor and technician portal
7. Internal PDF includes logistics costs; customer PDF does not
</verification>

<must_haves>
<truths>
- logistics_operations table exists in Supabase with RLS
- company_machines table exists in Supabase with RLS
- Daily rental price is auto-calculated as monthly/30
- Total logistics cost is auto-calculated as rental + pickup + return
- All date calculations use maintenance record visit dates (never browser/server time)
- Machine Logistics step appears between Supervisor and Notes in the editor
- Logistics section is always visible in record cards
- Three visual cards match the paidBy selector pattern
- Operation lookup is by customer
- Internal PDF includes all logistics costs
- Customer PDF hides all logistics costs
- Multiple simultaneous open operations are supported
- Existing records can be backfilled
- Both admin and technician portals include logistics
</truths>
<prohibitions>
- Never use browser Date.now(), new Date(), or server timestamps for business calculations — always use maintenanceDate
- Never show rental prices, daily rates, rental costs, or transportation costs in customer-facing PDFs
- Never add logisticsOperationId field to MaintenanceRecord type — record stays clean
</prohibitions>
</must_haves>

## Artifacts this phase produces

**Files created:**
- `supabase/migrations/*_create_logistics_tables.sql`
- `hooks/useLogisticsOperations.ts`
- `components/MachineLogisticsSection.tsx`
- `src/views/MachineSettingsView.tsx`
- `src/views/LogisticsTimelineView.tsx`

**Files modified:**
- `types.ts` — LogisticsOperation, CompanyMachine interfaces
- `constants.ts` — NAV_ITEMS entry for machines
- `components/MaintenanceRecordEditor.tsx` — stepper extension, logistics step
- `components/MaintenanceRecordCard.tsx` — logistics collapsible section
- `App.tsx` — lazy imports, view routing
- `src/views/Sidebar.tsx` — nav item + icon
- `utils/costAggregation.ts` — AggregatedCosts extension
- `utils/internalReportPdf.ts` — logistics section
- `utils/pdfGenerator.ts` — customer-facing logistics (no costs)
- `components/technician-portal/Step2_WorkLog.tsx` — logistics section

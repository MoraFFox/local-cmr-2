---
phase: 03-auto-remove-empty-rows-columns-sections-and-empty-states-fro
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - components/InternalReportPrintView.tsx
  - tests/i18n-allowlist.json
  - tests/internalReportPdf.test.ts
autonomous: true
requirements: [D-05, D-10, D-11]

must_haves:
  truths:
    - "HTML print view omits empty sections, rows, and columns with the same strictness as the PDF surfaces"
    - "Surviving empty cells in the print view render 'no data'"
    - "Missing-data capture flow still renders empty-field placeholders (D-05 regression guarded)"
  artifacts:
    - components/InternalReportPrintView.tsx renders conditionally on non-empty data
  key_links:
    - utils/pdfCompactLayout.ts isRowEmpty reused by print view
---

<objective>
Apply the same empty-state strictness (D-11) to the HTML print view `components/InternalReportPrintView.tsx`, and guard the D-05 hard boundary with a regression test so missing-data capture never loses its placeholders.

Purpose: Complete the D-10/D-11 surface coverage (all three report surfaces strict) and prove D-05 stays intact.
Output: Print view that renders only meaningful content; regression tests proving the missing-data flow is untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-RESEARCH.md
@components/InternalReportPrintView.tsx
@utils/missingDataPdf.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional rendering in InternalReportPrintView.tsx</name>
  <files>components/InternalReportPrintView.tsx, tests/i18n-allowlist.json</files>
  <read_first>
    - components/InternalReportPrintView.tsx — read the full component: FinancialCard (line 46), SectionTitle (line 67), PrintRecordCard (line 89), BranchInternalReport (line 204), the company report component, and every section's rendering condition
    - utils/pdfCompactLayout.ts — isRowEmpty / NO_DATA_LABEL helpers from plan 03-01
    - .planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md — D-01 (all report surfaces), D-05, D-10, D-11
  </read_first>
  <action>
    In components/InternalReportPrintView.tsx, per D-01/D-10/D-11:
    1. Empty sections: guard every section render (Branch Information, Visit Summary, Contact cards, Assigned Staff, Maintenance Log, Most Frequent Problems, Most Used Parts, Logistics, Financial Summary) so the section heading AND body are omitted when their data is empty/zero — same "vanish completely" rule as the PDF surfaces.
    2. Empty rows/columns in the print tables: for each table, drop entirely-empty rows (isRowEmpty) except maintenance-record rows (D-08 — keep every record row), and drop all-empty columns (mirror the buildCompactTable all-empty-column rule).
    3. "no data" placeholder (D-10): replace "—" placeholders in surviving empty cells with the NO_DATA_LABEL constant.
    4. Keep KPI cards rendering at 0 (D-06) — do not remove FinancialCard/KPI sections when their value is 0.
    5. Do NOT modify components used by the missing-data flow, and do not apply suppression logic inside utils/missingDataPdf.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/internalReportPdf.test.ts --reporter=dot && npx vitest run tests/i18nAudit.test.ts --reporter=dot</automated>
  </verify>
  <done>
    - Print view renders no empty section headings for absent data (component-level test or snapshot asserts absence)
    - Surviving empty cells render "no data"
    - KPI cards still render at 0
    - tests/i18n-allowlist.json contains the exact string "no data" (D-10 deliverable; added in plan 03-01, verify no duplicate)
    - npx tsc --noEmit passes
    - i18n audit passes (allowlist updated with "no data" and any new labels)
  </done>
</task>

<task type="auto">
  <name>Task 2: Missing-data D-05 regression test</name>
  <files>tests/internalReportPdf.test.ts</files>
  <read_first>
    - utils/missingDataPdf.ts — read generateMissingDataPDF (line 841) signature and what it renders
    - components/SubmissionDetails.tsx — read the missing-data call site (line 1383) for context
    - .planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md — D-05
  </read_first>
  <action>
    Add a regression test to tests/internalReportPdf.test.ts that proves D-05: the missing-data capture flow still renders empty-field placeholders after this phase's suppression work.
    1. Build a mock submission with missing fields (reuse generateMockWizardData and blank out fields).
    2. Call generateMissingDataPDF and assert the output still lists the missing fields (the drawn-strings capture asserts the missing-field labels or the placeholder text appear).
    3. Also assert the standard generated report on the SAME data does NOT show those placeholders — proving suppression is scoped to generated reports only.
    Use the existing vi.mock("jspdf") drawn-strings capture pattern already in the file.
  </action>
  <verify>
    <automated>npx vitest run tests/internalReportPdf.test.ts --reporter=dot</automated>
  </verify>
  <done>
    - A new test asserts generateMissingDataPDF output contains the missing-field placeholders
    - The same test asserts the standard generated report omits them
    - npx tsc --noEmit passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report-data → HTML print view | Data already validated upstream; suppression only filters presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | missing-data flow | mitigate | D-05 regression test asserts placeholders still render; suppression never applied in missingDataPdf.ts |
| T-03-22 | Information Disclosure | client print view | mitigate | Client mode must not re-expose cost cells; print view keeps existing includeCosts gating |
| T-03-SC | Tampering | npm/pypi/crates installs | n/a | No new packages installed this phase |
</threat_model>

## Artifacts this phase produces

- `components/InternalReportPrintView.tsx`: conditional section/row/column rendering, "no data" placeholder
- `tests/internalReportPdf.test.ts`: D-05 missing-data regression test (print-view empty-section assertions may live here or in a new component test)
- `tests/i18n-allowlist.json`: "no data" and any new labels

---
phase: 03-auto-remove-empty-rows-columns-sections-and-empty-states-fro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/pdfCompactLayout.ts
  - utils/internalReportPdf.ts
  - tests/internalReportPdf.test.ts
  - tests/i18n-allowlist.json
autonomous: true
requirements: [D-02, D-03, D-04, D-07, D-08, D-09, D-10, D-12]

must_haves:
  truths:
    - "Generated new-style PDFs render no 'No X' empty-section placeholder messages"
    - "A section with zero/empty content produces no section header at all"
    - "KPI cards render even when their value is 0"
    - "Maintenance record rows with only a date still render"
    - "Derived data rows (zones/technicians/parts/contacts) with all-empty content are omitted"
    - "Cost Breakdown section is omitted when every category total is 0"
    - "Surviving empty cells render 'no data' instead of '—'"
    - "Batch report cover drops zero-cost lines and empty selection description"
  artifacts:
    - utils/internalReportPdf.ts contains no unconditional drawEmptyMessage for empty sections
    - tests/i18n-allowlist.json contains the 'no data' string
  key_links:
    - utils/pdfCompactLayout.ts isValueEmpty used by all new-style table filters
---

<objective>
Centralize and enforce empty-state suppression for the new-style engine reports so every generated PDF renders only useful, meaningful information.

Purpose: Fulfill decisions D-02/D-03/D-04/D-07/D-08/D-09/D-10/D-12 for the engine-driven reports in `utils/internalReportPdf.ts`. The shared machinery added here (row predicate, "no data" placeholder, section gating) is reused by plan 02 (legacy `pdfGenerator.ts`) and plan 03 (HTML print view).
Output: Engine reports that drop empty sections/rows/columns, keep KPI cards and maintenance records, drop zero financial sections, and render "no data" in surviving empty cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-RESEARCH.md
@utils/pdfCompactLayout.ts
@utils/internalReportPdf.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared emptiness helpers and 'no data' placeholder to pdfCompactLayout.ts</name>
  <files>utils/pdfCompactLayout.ts, tests/internalReportPdf.test.ts</files>
  <read_first>
    - utils/pdfCompactLayout.ts — read isValueEmpty (line 31), IgnoreCondition (line 22), buildCompactTable (lines 253-274) before extending
    - utils/internalReportPdf.ts — read formatPartsList/formatServicesList/formatProblemsList (lines 28-100) to see where "—" is produced
    - .planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md — decisions D-02, D-03, D-07, D-08, D-10
  </read_first>
  <action>
    In utils/pdfCompactLayout.ts:
    1. Export a constant NO_DATA_LABEL = "no data" (per D-10). Do not reuse the literal elsewhere.
    2. Add an exported helper `isRowEmpty(row: Record<string, unknown>, columns: { accessor: (row) => unknown; ignoreIf: IgnoreCondition }[]): boolean` that returns true when every column's value is empty per its ignoreIf rule (route through the existing isValueEmpty). This is the D-07 row predicate and encodes the D-02 rule — 0, null, and empty all count as empty. Callers pass the D-08 exception for maintenance records by excluding the date/type/status columns from the row check or by skipping the filter for record tables.
    3. Add an exported `filterEmptyRows` helper that applies isRowEmpty and returns the surviving rows plus the count removed (for tests). Never mutate the input array.
    4. Ensure buildCompactTable's existing all-empty-column drop stays intact (do not regress it).
    Do NOT flip hideEmptyComponents to false anywhere. Do NOT touch the draft-mode path.
  </action>
  <verify>
    <automated>npx vitest run tests/internalReportPdf.test.ts --reporter=dot</automated>
  </verify>
  <done>
    - utils/pdfCompactLayout.ts exports NO_DATA_LABEL, isRowEmpty, and filterEmptyRows
    - isRowEmpty routes through isValueEmpty (grep confirms isValueEmpty call inside isRowEmpty)
    - isRowEmpty treats 0, null, and empty strings all as empty per D-02 (unit test covers each case)
    - tests/internalReportPdf.test.ts contains at least one unit test for isRowEmpty returning true for an all-empty row and false for a row with one non-empty cell
    - npx tsc --noEmit passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce section/row/column suppression and zero-drop in internalReportPdf.ts</name>
  <files>utils/internalReportPdf.ts, tests/internalReportPdf.test.ts</files>
  <read_first>
    - utils/internalReportPdf.ts — read drawEmptyMessage (line 313), the 15 drawEmptyMessage call sites (lines 858, 874, 949, 977, 1015, 1033, 1266, 1299, 1374, 1400, 1423, 1461, 1479, 2103, 2117), buildMaintenanceTableColumns (line 382+), renderMaintenanceHistoryTable, buildFinancialCategories (line 137), drawVisitDetails (line 1648), generateBatchReport cover (line 1950+)
    - utils/pdfTheme.ts — read drawFinancialSummary + drawSectionHeader signatures before gating Cost Breakdown
    - utils/pdfCompactLayout.ts — the helpers added in Task 1
  </read_first>
  <action>
    In utils/internalReportPdf.ts, per D-04/D-07/D-08/D-09/D-12:
    1. Empty sections: for every drawEmptyMessage call site that renders a "No X" placeholder for a section that has no data, replace the placeholder path with returning the section's start y unchanged (no header, no message). Where a section is built via engine.addRepeater, rely on the existing hideEmptyComponents skip (engine already drops the section header when all child blocks are empty) — remove the explicit drawEmptyMessage render. Where a section is built via engine.addSection + addBlock, guard the block so an empty result adds no block at all.
    2. KPI cards (D-06): leave buildKPICards untouched — cards keep rendering at 0.
    3. Maintenance records (D-08): the maintenance log table and per-record detail blocks must keep every record row, even when only the date is populated. Do not apply filterEmptyRows to record arrays.
    4. Derived data rows (D-07/D-08): apply filterEmptyRows to zone breakdown rows (0 visits), technician rows (no visits), parts rows (0 count), contact rows, and machine-fleet rows before drawing them.
    5. Financial section (D-09): gate the Cost Breakdown / Financial Summary block so it is not drawn when every category total is 0. Use categories.some((c) => c.total > 0) as the gate predicate.
    6. "no data" (D-10): replace the "—" placeholder produced in formatPartsList/formatServicesList/formatProblemsList and any surviving empty cell with the NO_DATA_LABEL constant imported from pdfCompactLayout.
    7. Batch cover (D-12): in generateBatchReport's Batch Summary info box, omit the total-cost line when totalCost is 0, and omit the Selection line when filterDescription is empty.
    Do NOT modify utils/missingDataPdf.ts. Do NOT change client/cost/internal mode cost separation.
  </action>
  <verify>
    <automated>npx vitest run tests/internalReportPdf.test.ts tests/BulkExportModal.test.tsx --reporter=dot</automated>
  </verify>
  <done>
    - No drawEmptyMessage "No X" call site remains reachable in a generated report for an empty section (grep the file; placeholder paths removed or guarded)
    - A generated report with no problems renders no "No problems" text and no "Most Frequent Problems" header (test asserts absence)
    - A report with zero-cost data renders no Cost Breakdown section (test asserts absence of "Cost Breakdown" and category totals)
    - Derived rows with 0 visits/0 count are absent from output (test asserts zone row with 0 visits absent)
    - Maintenance record with only a date still renders (test asserts presence)
    - Surviving empty cells show "no data" (test asserts string present; i18n audit passes)
    - KPI cards still render at 0 (D-06 positive assertion: a "Spare Parts: 0"-style card is present in output)
    - utils/missingDataPdf.ts is unmodified and no suppression logic was added to the draft path (D-05 guarded; diff of the file is empty)
    - Batch report cover omits "Total Cost:" line when total is 0
    - npx tsc --noEmit passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report-data → PDF | Record data already validated upstream; suppression only filters presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | client-mode PDFs | mitigate | Suppression must not re-expose cost cells in client mode — gate on existing clientMode flag; regression test asserts no cost strings in client output |
| T-03-02 | Tampering | draft/missing-data flow | mitigate | Never apply filters in missingDataPdf.ts; regression test asserts missing-data PDF still lists empty fields |
| T-03-SC | Tampering | npm/pypi/crates installs | n/a | No new packages installed this phase |
</threat_model>

## Artifacts this phase produces

- `utils/pdfCompactLayout.ts`: exports `NO_DATA_LABEL`, `isRowEmpty`, `filterEmptyRows`
- `utils/internalReportPdf.ts`: section-gating changes, derived-row filters, financial zero-gate, "no data" placeholder, batch-cover omits
- `tests/internalReportPdf.test.ts`: row-drop / financial-zero / "no data" / maintenance-record-kept / empty-section tests
- `tests/i18n-allowlist.json`: adds "no data"

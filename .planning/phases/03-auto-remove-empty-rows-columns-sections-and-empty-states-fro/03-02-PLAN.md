---
phase: 03-auto-remove-empty-rows-columns-sections-and-empty-states-fro
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - utils/pdfGenerator.ts
  - tests/pdfGeneratorEmpty.test.ts
  - tests/i18n-allowlist.json
autonomous: true
requirements: [D-02, D-07, D-08, D-10, D-11]

must_haves:
  truths:
    - "Legacy company and branch PDFs omit entirely-empty rows and columns in every table"
    - "Maintenance record rows with only a date still render in legacy tables"
    - "Surviving empty cells in legacy tables render 'no data'"
    - "Legacy PDFs produce no jsPDF autoTable width-overflow warnings for filtered tables"
  artifacts:
    - tests/pdfGeneratorEmpty.test.ts exists and passes
  key_links:
    - utils/pdfCompactLayout.ts isRowEmpty reused by legacy table filters
---

<objective>
Apply the same empty-state strictness (D-11) to the legacy generator `utils/pdfGenerator.ts` — `generateCompanyPDF` (line 508) and `generateBranchPDF` (line 1227) — whose 15 raw `autoTable` calls currently render empty rows and columns.

Purpose: Fulfill D-02/D-07/D-08/D-10/D-11 for the legacy surface. Reuse the shared helpers from plan 03-01 (isRowEmpty, NO_DATA_LABEL) so all three surfaces share one vocabulary.
Output: Legacy PDFs that filter empty rows/columns before drawing and render "no data" in surviving empty cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md
@.planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-RESEARCH.md
@utils/pdfGenerator.ts
@utils/pdfCompactLayout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter empty rows and columns in every legacy autoTable call</name>
  <files>utils/pdfGenerator.ts, tests/pdfGeneratorEmpty.test.ts</files>
  <read_first>
    - utils/pdfGenerator.ts — read generateCompanyPDF (line 508) and generateBranchPDF (line 1227) fully; inspect all 15 autoTable call sites (lines 717, 774, 803, 851, 922, 973, 1047, 1099, 1125, 1165, 1288, 1331, 1364, 1490, 1520) to see each table's columns, body arrays, and columnStyles
    - utils/pdfCompactLayout.ts — the isRowEmpty / NO_DATA_LABEL helpers from plan 03-01
    - utils/pdfTheme.ts — pdfText usage for any cell-text shaping needs
    - .planning/phases/03-auto-remove-empty-rows-columns-sections-and-empty-states-fro/03-CONTEXT.md — D-01 (all report surfaces), D-07, D-08, D-10, D-11
  </read_first>
  <action>
    In utils/pdfGenerator.ts, per D-01/D-07/D-08/D-10/D-11:
    1. For each of the 15 autoTable call sites, restructure so the table is built from a filtered column set and filtered body:
       a. Define each table's columns with an accessor + ignoreIf (reusing the IgnoreCondition vocabulary).
       b. Drop all-empty columns first (reuse the buildCompactTable all-empty-column pattern: keep a column when at least one row has a non-empty value for it, or ignoreIf === "never").
       c. Drop entirely-empty rows (isRowEmpty) EXCEPT maintenance-record rows — for tables whose rows are maintenance records, keep every row (D-08); for derived tables (zones, technicians, parts, machines, contacts, branch comparisons), drop rows where every surviving cell is empty.
       d. Rebuild columnStyles/widths from the surviving column set so the table fills the page width (mirror the widening approach) — this prevents autoTable width-overflow warnings.
    2. Replace "—" placeholders in legacy cell strings with the NO_DATA_LABEL constant for surviving empty cells (D-10).
    3. KPI/summary cards in the legacy generator follow D-06 (keep at 0) — do not remove them.
    4. Do NOT touch utils/missingDataPdf.ts or any draft-mode rendering.
    5. Keep client-mode cost stripping intact (includeCosts:false) — suppression must not re-expose costs.
  </action>
  <verify>
    <automated>npx vitest run tests/pdfGeneratorEmpty.test.ts tests/internalReportPdf.test.ts --reporter=dot</automated>
  </verify>
  <done>
    - tests/pdfGeneratorEmpty.test.ts exists and passes: asserts a legacy company PDF omits an all-empty zone row and an all-empty column, keeps a date-only maintenance record, and renders "no data" in a surviving empty cell
    - No autoTable width-overflow warning appears in the test output for filtered tables
    - npx tsc --noEmit passes
    - npx vitest run tests/i18nAudit.test.ts passes (allowlist updated if needed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report-data → PDF | Record data already validated upstream; suppression only filters presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | legacy client PDFs | mitigate | Suppression must not re-expose cost cells in client mode (includeCosts:false); regression test asserts no cost strings |
| T-03-12 | Tampering | columnStyles/width drift | mitigate | Rebuild widths from surviving columns; test asserts no width-overflow warnings |
| T-03-SC | Tampering | npm/pypi/crates installs | n/a | No new packages installed this phase |
</threat_model>

## Artifacts this phase produces

- `utils/pdfGenerator.ts`: filtered column/row sets and rebuilt columnStyles at all 15 autoTable call sites; "no data" placeholder
- `tests/pdfGeneratorEmpty.test.ts`: legacy empty-filtering tests
- `tests/i18n-allowlist.json`: "no data" already added in plan 03-01 (no duplicate)
